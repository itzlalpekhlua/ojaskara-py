node_modules/
.next/
out/
*.tsbuildinfo
*.log

# Local dev-tool config (absolute machine paths) — not portable across clones.
.claude/

backend/venv/
backend/__pycache__/
backend/**/__pycache__/
backend/.env

frontend/.env.local

# Admin-uploaded media and JSON "database" — real content, not sample code.
# Comment these two lines out if you want to version-control real site data.
backend/database/images/
backend/database/*.json
